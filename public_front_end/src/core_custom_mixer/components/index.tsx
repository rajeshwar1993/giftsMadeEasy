import CoreComponents from '../../core/components';

import CustomComponents from '../../custom_client_code/component_overrides';

const AllComponents = {
  ...CoreComponents,
  ...CustomComponents
};

export default AllComponents;
